<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>

<meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
  
<style>
	nav{
	margin : 15px;
	border-radius: 30px 30px 30px 30px ;
	height : 10%;
	opacity : 1; 
	}
	
	#a, #b{
	border-radius: 0px;
		}
	#searchbtn{
	border-radius: 0px 30px 30px 0px ;
	margin-left: -20px;
	}
	#search{
	border-radius: 30px 0px 0px 30px ;
	margin-left: -20px;
	}
	#button1 {
	border-radius: 30px 0px 0px 30px ;}
	#button2 {
	border-radius: 0px 30px 30px 0px ;}
</style>

</head>
<body>
<body>
<nav class="navbar navbar-expand-sm navbar-dark bg-success fixed-top ">
  <div class="container-fluid">
   <img alt="logo" src="img/logo.png" width="9%" height="9%">
    
    <div class="collapse navbar-collapse" id="mynavbar">
      <ul class="navbar-nav me-auto">
        <li class="">
			<div class="btn-group">
				<button type="button" class="btn btn-primary" id="button1">건강정보</button>
				<div class="btn-group">
					<button type="button" class="btn btn-primary dropdown-toggle" data-bs-toggle="dropdown" id="a" onclick="javascrirpt:location.href='${pageContext.request.contextPath }/'">자가건강 테스트</button>
					<div class="dropdown-menu">
						<a class="dropdown-item" href="${pageContext.request.contextPath }/selfcheck/bmi.jsp">테스트 하기</a>
						<a class="dropdown-item" href="${pageContext.request.contextPath }/">테스트 결과</a>
				</div>
				<div class="btn-group">
					<button type="button" class="btn btn-primary dropdown-toggle" data-bs-toggle="dropdown" id="b">약정보</button>
						<div class="dropdown-menu">
							<a class="dropdown-item" href="${pageContext.request.contextPath }/">약 검색</a>
							<a class="dropdown-item" href="${pageContext.request.contextPath }/">병원 검색</a>
				</div>
				<button type="button" class="btn btn-primary" onclick="javascrirpt:location.href='http://naver.com'">병원정보</button>
				<button type="button" class="btn btn-primary" id="button2"onclick="javascrirpt:location.href='${pageContext.request.contextPath }/'">자유게시판</button>
			</div>
        </li>
        <li class="">
          <a class="nav-link" href="javascript:void(0)">.</a>
        </li>
      </ul>
      <form class="d-flex">
        <input class="form-control me-2" type="text" placeholder="약, 병원 검색"  id="search">
        <button class="btn btn-primary" type="button" id="searchbtn">Search</button>
      </form>
    </div>
  </div>
</nav>

<div class="container-fluid mt-3">
<br><br><br><br>
  <h1>INDEX</h1>
  <p>You canthe navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation bar.the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation bar.the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation bar.the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation bar.the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation bar.the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation bar.the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation bar.the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation bar. also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation barYou can also include forms inside the navigation bar.</p>
  
</div>
</body>
</body>
</html>